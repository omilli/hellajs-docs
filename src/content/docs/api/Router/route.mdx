---
title: route
description: Reactive signal containing current route info.
---

## route

A reactive signal containing the current route information. Use it to reactively access the current path, params, query, and handler.

```typescript
/**
 * The current route info as a reactive signal.
 * 
 * {
 *   handler: Function | null,
 *   params: Record<string, string>,
 *   query: Record<string, string>,
 *   path: string
 * }
 */
route()
```

### Example

```typescript
import { route, effect } from "@hellajs/core";

effect(() => {
  const r = route();
  console.log("Current path:", r.path);
  console.log("Params:", r.params);
  console.log("Query:", r.query);
  // Use r.handler to render the current route's component
});
```

